product: 1.0.0
info:
  name: websocket-test
  title: websocket-test
  version: 1.0.0

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

apis:
  websocket-test:
    $ref: websocket-test.yaml

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
